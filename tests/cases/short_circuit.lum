# && must not evaluate the right side when the left is false.
# If it did, 100 / x would divide by zero. Printing 0 proves short-circuiting.
fn main() {
    let x = 0;
    if (x != 0 && 100 / x > 0) {
        print 1;
    } else {
        print 0;
    }
    return 0;
}
