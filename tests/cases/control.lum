# if/else inside a while loop; prints even numbers as-is, odds negated
fn main() {
    let i = 1;
    while (i <= 5) {
        if (i % 2 == 0) {
            print i;
        } else {
            print -i;
        }
        i = i + 1;
    }
    return 0;
}
