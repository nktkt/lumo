# フィボナッチ数列を計算する Lumo プログラム
# 再帰関数・if・while・print が動くことを確認できる

fn fib(n) {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    let i = 0;
    while (i < 15) {
        print fib(i);
        i = i + 1;
    }
    return 0;
}
